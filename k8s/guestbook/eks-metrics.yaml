apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana
  namespace: argocd
  labels:
    grafana_dashboard: "1"
annotations:
  grafana_folder: "EKS Cluster"
data:
  eks-dashboard.json: |-
    {
      "annotations": { "list": [] },
      "editable": true,
      "fiscalYearStartMonth": 0,
      "graphTooltip": 0,
      "id": null,
      "title": "ECS Core Metrics",
      "panels": [
        {
          "type": "gauge",
          "title": "Sample Metric Panel",
          "id": 1,
          "targets": [
            {
              "datasource": { "type": "cloudwatch", "uid": "aws-cloudwatch" },
              "expression": "SELECT AVG(CPUUtilization) FROM SCHEMA(\"AWS/ECS\", ClusterName)",
              "id": "A"
            }
          ]
        }
      ]
    }